<%= form_for(grandma_enjoys_music) do |f| %>
  <% if grandma_enjoys_music.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(grandma_enjoys_music.errors.count, "error") %> prohibited this grandma_enjoys_music from being saved:</h2>

      <ul>
      <% grandma_enjoys_music.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :form %>
    <%= f.text_field :form %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
